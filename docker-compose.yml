version: '3.7'

services:
  app:
    build: .
    volumes:
      - "code:/var/www/app"
  web:
    image: nginx:latest
    volumes:
      - "./docker/web/nginx-default.conf:/etc/nginx/conf.d/default.conf:ro"
      - "code:/var/www/app:ro"
    depends_on:
      - app

volumes:
  code:
